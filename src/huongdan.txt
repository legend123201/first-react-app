- components: là từ video đầu tiên về react, làm về counter, và dùng class component.

- day 5: làm về export, export default và cách import.

- day 6: làm về thêm xóa sửa todoList, interval và giải thích về state.

- day 10: làm về route, thêm xóa sửa todolist với ko có và có api. Ở đây xài api "ko dùng redux", dùng tốt cho trường hợp gọi api gán cho state thôi chứ ko phải global state (cái này cũng ko chắc vì mình còn phải kiểm tra status, phải catch lỗi, ko biết nên dùng cách này hay là dùng myCallBack tiếp).

- day 15: làm về thêm xóa sửa todoList với redux + api.

- day 17: code của anh Huy về mui. Custom hook, common-components. có thêm xóa sửa userList bằng api và ko bằng api + redux + tách thêm actionReducer.

- cai tien day 17: sử dụng "reusing reducer logic" (1 trường hợp hay của redux, có trên trang redux). Tạo 2 danh sách User và Todo và làm việc trên 2 ds đó.